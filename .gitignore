/bin/
/authcli

# Local environment overrides, never commit real secrets.
.env

# Readline history when the CLI is run outside a container.
.authcli_history

# Uploaded task description, not part of the deliverable.
/uploads/
